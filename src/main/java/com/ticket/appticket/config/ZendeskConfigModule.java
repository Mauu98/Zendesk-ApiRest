package com.ticket.appticket.config;


public class ZendeskConfigModule{

    /*protected void configure() {
        bind(ZendeskConfig.class)
                .annotatedWith(Names.named("Authorized"))
                .to(ZendeskConfigFromProperties.class);
    }*/
}
